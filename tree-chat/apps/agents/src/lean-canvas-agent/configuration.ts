/**
 * Configuration schema for lean canvas agent.
 * Currently no configuration is needed, but this file maintains consistency with other agents.
 */
export const ConfigurationSchema = {
  type: "object",
  properties: {},
  additionalProperties: false,
} as const;